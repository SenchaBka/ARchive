// Tanstack Query + Auth providers

'use client';

import type { PropsWithChildren } from "react";

// Simple provider setup without external dependencies for now
export default function Providers({ children }: PropsWithChildren) {
  return <>{children}</>;
}